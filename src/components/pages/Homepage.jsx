import React from "react";
import {Button} from "semantic-ui-react";

const Homepage = () => (
    <div>
        <h1>Homepage</h1>
        <Button primary >Signup</Button> or  <Button primary > Login</Button>
    </div>
)

export default Homepage;